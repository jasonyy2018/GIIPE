# 修复15分钟后首页打不开问题

## 问题描述

前端Docker运行15分钟后，首页就打不开了。

## 可能的原因

### 1. 内存泄漏（最可能）

**症状**:
- 运行一段时间后内存使用持续增长
- 最终内存耗尽导致进程崩溃或卡住
- SSR渲染变慢或卡住

**原因**:
- 未清理的定时器（setTimeout/setInterval）
- 未清理的事件监听器
- 未清理的Promise
- React组件内存泄漏

**检查**:
```bash
# 检查内存使用趋势
docker stats conference-frontend-prod

# 检查进程数
docker exec conference-frontend-prod ps aux | grep node | wc -l
```

### 2. SSR渲染卡住累积

**症状**:
- 每次SSR渲染都可能卡住
- 累积的卡住请求导致服务器无响应
- 15分钟后达到临界点

**原因**:
- Promise.race无法真正取消请求
- API请求超时但未真正取消
- 累积的未完成请求

**检查**:
```bash
# 检查SSR日志
docker logs --tail 100 conference-frontend-prod | grep SSR
```

### 3. 进程崩溃但容器未重启

**症状**:
- Node.js进程崩溃
- 容器还在运行但进程不存在
- 健康检查失败

**原因**:
- 内存不足导致OOM kill
- 未捕获的错误导致进程退出
- 资源限制

**检查**:
```bash
# 检查进程
docker exec conference-frontend-prod ps aux | grep node

# 检查容器重启次数
docker inspect conference-frontend-prod | grep RestartCount
```

### 4. 连接数过多

**症状**:
- 大量未关闭的连接
- 服务器无法处理新请求
- 15分钟后达到连接数限制

**原因**:
- HTTP连接未正确关闭
- 长连接累积
- 连接池耗尽

**检查**:
```bash
# 检查网络连接
docker exec conference-frontend-prod netstat -an | grep :3000 | wc -l
```

### 5. 资源限制

**症状**:
- CPU或内存达到限制
- 进程被限制或杀死
- 响应变慢或失败

**原因**:
- Docker资源限制过低
- 系统资源不足

**检查**:
```bash
# 检查资源使用
docker stats --no-stream conference-frontend-prod
```

## 诊断步骤

运行诊断脚本：

```bash
bash 诊断15分钟后首页打不开.sh
```

## 修复方案

### 方案1: 应用内存泄漏修复（推荐）

```bash
bash 应用崩溃修复.sh
```

这会修复：
- SSR超时Promise清理
- 客户端重试定时器清理
- 防止内存泄漏

### 方案2: 设置定时重启（临时）

```bash
bash 设置定时重启-临时方案.sh
```

设置每天凌晨3点自动重启，避免15分钟后问题。

### 方案3: 增加内存限制（缓解）

在 `docker-compose.prod.yml` 中：
```yaml
deploy:
  resources:
    limits:
      memory: 4G  # 从3G增加到4G
```

然后重启：
```bash
docker-compose -f docker-compose.prod.yml up -d frontend
```

### 方案4: 重启容器（立即）

```bash
docker-compose -f docker-compose.prod.yml restart frontend
```

## 根本原因分析

根据之前的修复，我们已经修复了：
- ✅ SSR超时Promise清理
- ✅ 客户端重试定时器清理

但可能还有其他内存泄漏源：
1. **事件监听器未清理**
2. **React组件内存泄漏**
3. **API请求未正确取消**

## 建议

1. **立即**: 重启容器恢复服务
2. **短期**: 设置定时重启（每12小时）
3. **长期**: 应用所有内存泄漏修复（需要重构）

## 监控建议

设置监控，在问题发生前预警：

```bash
# 监控内存使用
watch -n 30 'docker stats --no-stream conference-frontend-prod | tail -1'

# 监控进程数
watch -n 30 'docker exec conference-frontend-prod ps aux | grep node | wc -l'
```

