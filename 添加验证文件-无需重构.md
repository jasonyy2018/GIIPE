# 添加腾讯验证文件 - 无需重构方法

## 方法一：直接复制文件到容器（推荐，最快）

无需重构，直接将验证文件复制到运行中的容器：

```bash
# 1. 将验证文件复制到容器内的public目录
docker cp 你的验证文件.txt conference-frontend-prod:/app/public/

# 2. 重启前端容器（让Next.js重新加载public目录）
docker-compose -f docker-compose.prod.yml restart frontend
```

**示例：**
```bash
# 假设验证文件名为 MP_verify_abc123.txt
docker cp MP_verify_abc123.txt conference-frontend-prod:/app/public/

# 重启前端
docker-compose -f docker-compose.prod.yml restart frontend
```

**验证：**
```bash
# 测试文件是否可以访问
curl http://giip.info/MP_verify_abc123.txt
```

---

## 方法二：添加Volume挂载（永久方案）

修改docker-compose.prod.yml，添加public目录的volume挂载，以后添加文件就不需要重启了：

### 步骤：

1. **修改 docker-compose.prod.yml**

在frontend服务的volumes部分添加：
```yaml
frontend:
  # ... 其他配置 ...
  volumes:
    - ./frontend/public:/app/public:ro  # 添加这一行
```

2. **重启前端服务**
```bash
docker-compose -f docker-compose.prod.yml up -d frontend
```

3. **以后添加验证文件**
   - 直接将文件放到 `frontend/public/` 目录
   - 无需重启，文件立即可用

---

## 方法三：通过Nginx直接提供（最简单）

如果验证文件不需要通过Next.js处理，可以直接通过Nginx提供：

### 步骤：

1. **创建验证文件目录**
```bash
mkdir -p nginx/verify
```

2. **将验证文件放到nginx/verify目录**
```bash
cp 你的验证文件.txt nginx/verify/
```

3. **修改 nginx/conf.d/default.conf**

在server块中添加：
```nginx
# 验证文件直接由Nginx提供（无需通过Next.js）
location ~* ^/.*\.txt$ {
    root /etc/nginx/verify;
    try_files $uri =404;
    access_log off;
    expires 1h;
    add_header Cache-Control "public";
}
```

4. **修改 docker-compose.prod.yml**

在nginx服务的volumes部分添加：
```yaml
nginx:
  # ... 其他配置 ...
  volumes:
    - ./nginx/verify:/etc/nginx/verify:ro  # 添加这一行
```

5. **重启nginx**
```bash
docker-compose -f docker-compose.prod.yml restart nginx
```

---

## 推荐方案

- **临时/快速**：使用方法一（docker cp）
- **长期使用**：使用方法二（添加volume挂载）
- **完全独立**：使用方法三（Nginx直接提供）

## 注意事项

- 方法一和方法二需要重启前端容器
- 方法三只需要重启nginx，不影响前端
- 所有方法都不需要重新构建镜像

