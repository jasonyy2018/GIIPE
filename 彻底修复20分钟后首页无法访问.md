# 彻底修复20分钟后首页无法访问

## 问题确认

用户反馈：
- ✅ SSR正常（数据加载成功）
- ✅ 响应时间正常（0.155s）
- ❌ **20分钟后首页无法正常访问**
- ✅ 其他页面访问正常

这说明问题在**首页特定的配置或缓存机制**。

## 关键问题发现

### 问题1: Next.js ISR (Incremental Static Regeneration) 累积

**文件**: `frontend/src/lib/server-api.ts`

**问题**:
```typescript
next: { revalidate: 60 }, // Revalidate every 60 seconds
```

**关键问题**:
1. **ISR每60秒重新验证一次**
2. 每次重新验证都会触发后台fetch请求
3. 如果有很多请求，会累积很多后台任务
4. **20分钟后累积到临界点**，导致页面无法访问

**为什么其他页面正常？**
- 其他页面可能不使用ISR
- 或者使用方式不同，不会累积

### 问题2: 首页没有禁用缓存

**文件**: `frontend/src/app/page.tsx`

**问题**:
- 首页没有设置 `dynamic = 'force-dynamic'`
- Next.js可能会缓存页面
- ISR的revalidate可能导致累积问题

## 已应用的修复

### 修复1: 禁用首页缓存和ISR

**文件**: `frontend/src/app/page.tsx`

```typescript
// CRITICAL: Disable caching and ISR for homepage to prevent accumulation issues
export const dynamic = 'force-dynamic';
export const revalidate = 0; // Disable ISR completely
```

**效果**:
- ✅ 强制动态渲染，不缓存
- ✅ 完全禁用ISR
- ✅ 每次请求都重新渲染
- ✅ 不会累积后台任务

### 修复2: 禁用API请求缓存

**文件**: `frontend/src/lib/server-api.ts`

```typescript
// 修复前（错误）:
next: { revalidate: 60 }, // Revalidate every 60 seconds

// 修复后（正确）:
cache: 'no-store', // Always fetch fresh data, no caching
```

**效果**:
- ✅ 完全禁用缓存
- ✅ 每次请求都获取最新数据
- ✅ 不会累积ISR任务
- ✅ 不会累积后台验证

## 应用修复

运行修复脚本：

```bash
bash 应用内存泄漏修复.sh
```

或手动执行：

```bash
# 重新构建前端（包含最终修复）
docker-compose -f docker-compose.prod.yml build --no-cache frontend

# 重启前端容器
docker-compose -f docker-compose.prod.yml up -d frontend
```

## 为什么这个修复有效

1. **禁用ISR**
   - ISR每60秒重新验证，会累积后台任务
   - 20分钟后累积到临界点
   - 禁用后不会累积

2. **禁用缓存**
   - Next.js缓存可能导致问题
   - 强制动态渲染确保每次都是新请求
   - 不会累积缓存任务

3. **完全禁用**
   - `dynamic = 'force-dynamic'` 强制动态
   - `revalidate = 0` 禁用ISR
   - `cache: 'no-store'` 禁用HTTP缓存
   - 确保不会有任何累积

## 验证修复

修复后，首页应该：
1. ✅ 不会累积ISR任务
2. ✅ 不会累积后台验证
3. ✅ 20分钟后仍然正常访问
4. ✅ 内存使用稳定

测试：
```bash
# 等待20分钟后测试
sleep 1200  # 20分钟
time docker exec conference-frontend-prod wget -q -O- --timeout=15 http://127.0.0.1:3000/ > /dev/null

# 应该仍然快速响应（<1秒）
```

## 总结

这个修复解决了**根本原因**：
- ❌ 之前：ISR每60秒重新验证，累积后台任务
- ✅ 现在：完全禁用ISR和缓存，不会累积

这应该能彻底解决20分钟后首页无法访问的问题。

