# 修复前端3000端口无响应问题

## 可能的原因

1. **内存不足（OOM）** - 2G内存限制可能不够
2. **Node.js进程崩溃** - 未捕获的错误导致进程退出
3. **健康检查失败** - 健康检查配置过于严格
4. **日志文件过大** - 导致磁盘空间不足
5. **内存泄漏** - 长时间运行导致内存耗尽
6. **自动重启失败** - 容器重启但启动失败

## 解决方案

### 方案1：增加内存限制和添加自动重启机制

修改 `docker-compose.prod.yml`：

```yaml
frontend:
  # ... 其他配置 ...
  deploy:
    resources:
      limits:
        cpus: '2'
        memory: 3G  # 从2G增加到3G
      reservations:
        cpus: '1'
        memory: 1.5G  # 从1G增加到1.5G
  restart: always  # 从unless-stopped改为always，确保总是重启
```

### 方案2：改进健康检查配置

当前健康检查可能过于严格，修改为更宽松的配置：

```yaml
healthcheck:
  test: ["CMD", "sh", "-c", "wget --no-verbose --tries=1 --spider --timeout=10 http://127.0.0.1:3000/ 2>&1 | grep -qE '200|304|saved|HTTP' || (curl -f -s --max-time 10 http://127.0.0.1:3000/ >/dev/null 2>&1) || exit 1"]
  interval: 30s
  timeout: 20s  # 从15s增加到20s
  retries: 3  # 从5减少到3，避免频繁重启
  start_period: 300s
```

### 方案3：添加Node.js内存限制和进程管理

修改 `frontend/Dockerfile.prod` 的启动命令：

```dockerfile
# 添加Node.js内存限制和更好的错误处理
CMD ["sh", "-c", "echo 'Starting Next.js server...' && NODE_OPTIONS='--max-old-space-size=2048' node server.js"]
```

### 方案4：添加进程监控和自动恢复

创建一个启动脚本 `frontend/start.sh`：

```bash
#!/bin/sh
set -e

echo "Starting Next.js server with monitoring..."

# 设置Node.js内存限制
export NODE_OPTIONS="--max-old-space-size=2048"

# 使用无限循环确保进程总是运行
while true; do
    echo "Starting Next.js server..."
    node server.js || {
        echo "Next.js server crashed, restarting in 5 seconds..."
        sleep 5
    }
done
```

然后在Dockerfile中使用：

```dockerfile
COPY start.sh /app/start.sh
RUN chmod +x /app/start.sh
CMD ["/app/start.sh"]
```

### 方案5：添加日志轮转和清理

确保日志不会无限增长，在docker-compose中已经配置了日志限制，但可以进一步优化：

```yaml
logging:
  driver: "json-file"
  options:
    max-size: "10m"
    max-file: "3"
    compress: "true"  # 添加压缩
```

## 推荐的完整修复方案

结合以上方案，创建一个完整的修复：

1. **增加内存限制**（3G）
2. **改进健康检查**（更宽松）
3. **添加Node.js内存限制**
4. **使用always重启策略**
5. **添加进程监控**

## 快速修复命令

```bash
# 1. 检查当前状态
docker ps -a | grep frontend
docker logs --tail 100 conference-frontend-prod

# 2. 检查内存使用
docker stats --no-stream conference-frontend-prod

# 3. 如果容器已停止，手动重启
docker-compose -f docker-compose.prod.yml restart frontend

# 4. 如果重启失败，查看日志
docker-compose -f docker-compose.prod.yml logs frontend --tail 200
```

## 预防措施

1. **定期检查容器状态**
2. **监控内存使用**
3. **设置告警**
4. **定期清理日志**

