# 修复运行一段时间后崩溃问题

## 问题分析

用户反馈：重构后正常，但运行一段时间后就不正常了，通过映射的3000端口无法访问网站。

可能原因：
1. **内存泄漏**：未清理的定时器导致内存累积
2. **SSR超时Promise未清理**：`setTimeout`创建的定时器在Promise.race完成后未清理
3. **客户端重试定时器未清理**：`FeaturedContentClient`中的重试逻辑未清理定时器
4. **进程挂起**：虽然进程还在运行，但不响应请求
5. **内存耗尽**：长时间运行后内存不足导致OOM

## 已修复的问题

### 1. SSR超时Promise清理
**文件**：`frontend/src/app/page.tsx`
- ✅ 添加了`timeoutId`跟踪
- ✅ 在Promise.race完成后清理定时器
- ✅ 防止内存泄漏

### 2. 客户端重试定时器清理
**文件**：`frontend/src/components/public/FeaturedContentClient.tsx`
- ✅ 添加了`retryTimeoutId`跟踪
- ✅ 在useEffect cleanup中清理定时器
- ✅ 防止组件卸载后定时器继续运行

## 建议的额外措施

### 1. 添加进程监控和自动重启
在`docker-compose.prod.yml`中：
- 使用`restart: always`（已设置）
- 添加健康检查（已设置）
- 考虑添加进程监控脚本

### 2. 增加内存限制
当前设置：`--max-old-space-size=2048`（2GB）
如果仍然崩溃，可以：
- 增加到3GB：`--max-old-space-size=3072`
- 检查是否有内存泄漏

### 3. 添加日志监控
监控以下日志：
- SSR超时警告
- 内存使用情况
- 进程崩溃原因

### 4. 定期重启策略
如果问题持续，可以考虑：
- 使用cron定期重启容器
- 或使用Docker的自动重启策略

## 应用修复

运行以下命令应用修复：

```bash
# 重新构建前端
docker-compose -f docker-compose.prod.yml build --no-cache frontend

# 重启前端容器
docker-compose -f docker-compose.prod.yml up -d frontend

# 监控日志
docker logs -f conference-frontend-prod
```

## 验证修复

1. **检查内存使用**：
   ```bash
   docker stats conference-frontend-prod
   ```

2. **检查进程状态**：
   ```bash
   docker exec conference-frontend-prod ps aux | grep node
   ```

3. **测试访问**：
   ```bash
   curl -I http://localhost:3000/
   ```

4. **监控日志**：
   ```bash
   docker logs --tail 100 conference-frontend-prod | grep -E "timeout|error|memory"
   ```

## 如果问题仍然存在

1. **检查系统资源**：
   - CPU使用率
   - 内存使用率
   - 磁盘空间

2. **检查后端API响应时间**：
   - 如果后端API慢，会导致SSR超时
   - 检查后端日志

3. **考虑禁用SSR**：
   - 如果SSR持续导致问题，可以考虑完全使用客户端渲染
   - 修改`page.tsx`为客户端组件

4. **添加进程监控**：
   - 使用PM2或类似工具监控Node.js进程
   - 自动重启崩溃的进程

