# 深入检查首页崩溃问题

## 问题描述

- ✅ 重构后可以恢复一段时间
- ❌ 过一段时间后首页无法访问
- ❌ 重启前端也无效
- ❌ 重构只能恢复一段时间，又会变成无法访问

这说明问题**不是简单的SSR超时**，而是**更深层的资源泄漏或进程崩溃**。

## 关键问题发现

### 问题1: FeaturedContentClient 的 useEffect 依赖项问题

**文件**: `frontend/src/components/public/FeaturedContentClient.tsx`

**问题**:
```typescript
useEffect(() => {
  // ... fetch logic
}, []); // 空依赖数组
```

但是：
1. `initialEvents` 和 `initialConferences` 在依赖数组中缺失
2. 如果这些props变化，useEffect不会重新运行
3. 但更重要的是：**如果组件重新挂载，会创建新的fetch请求**

### 问题2: Promise.race 中的 setTimeout 未清理

**文件**: `frontend/src/app/page.tsx`

**问题**:
```typescript
const timeoutPromise = new Promise<{events: Event[], conferences: Event[]}>((resolve) => {
  setTimeout(() => {
    resolve({ events: [], conferences: [] });
  }, SSR_TIMEOUT);
});
```

**关键问题**:
- `setTimeout` 创建的定时器**没有清理机制**
- 如果 `getHomepageData()` 在超时前完成，`setTimeout` 仍然会执行
- 虽然不会影响结果，但会**累积未清理的定时器**
- 每次SSR都会创建一个新的定时器，15分钟后可能有数百个未清理的定时器

### 问题3: 客户端 fetch 请求可能累积

**文件**: `frontend/src/components/public/FeaturedContentClient.tsx`

**问题**:
- 如果组件频繁重新挂载（例如路由变化、状态更新）
- 每次挂载都会创建新的 fetch 请求
- 如果请求慢或超时，可能累积大量未完成的请求

### 问题4: Next.js 服务器配置问题

**文件**: `frontend/next.config.js`

**问题**:
- `output: 'standalone'` - 这可能导致内存问题
- 没有设置 `maxMemory` 或 `maxOldSpaceSize` 在 Next.js 配置中
- `reactStrictMode: true` - 在开发环境会导致双重渲染，但在生产环境应该没问题

### 问题5: Docker 健康检查可能不够严格

**文件**: `docker-compose.prod.yml`

**问题**:
- 健康检查只检查 `/api/health` 端点
- 如果首页SSR卡住，健康检查可能仍然通过
- 健康检查失败后，容器会重启，但问题可能已经累积

## 修复方案

### 修复1: 清理 Promise.race 中的 setTimeout

**文件**: `frontend/src/app/page.tsx`

```typescript
// 创建超时Promise，但确保可以清理
let timeoutId: NodeJS.Timeout | null = null;
const timeoutPromise = new Promise<{events: Event[], conferences: Event[]}>((resolve) => {
  timeoutId = setTimeout(() => {
    console.warn('[Homepage SSR] Timeout reached after 5s, rendering with empty data');
    resolve({ events: [], conferences: [] });
  }, SSR_TIMEOUT);
});

try {
  const homepageData = await Promise.race([
    getHomepageData().finally(() => {
      // 如果数据获取成功，清理超时定时器
      if (timeoutId) {
        clearTimeout(timeoutId);
        timeoutId = null;
      }
    }),
    timeoutPromise
  ]);
  
  // 确保清理
  if (timeoutId) {
    clearTimeout(timeoutId);
    timeoutId = null;
  }
  
  initialEvents = homepageData.events || [];
  initialConferences = homepageData.conferences || [];
} catch (error) {
  // 确保清理
  if (timeoutId) {
    clearTimeout(timeoutId);
    timeoutId = null;
  }
  // ... error handling
}
```

### 修复2: 改进 FeaturedContentClient 的 useEffect

**文件**: `frontend/src/components/public/FeaturedContentClient.tsx`

```typescript
useEffect(() => {
  // ... existing code ...
  
  let isMounted = true; // 跟踪组件是否已挂载
  let abortController: AbortController | null = null;
  
  const fetchData = async (isRetry = false) => {
    // 如果组件已卸载，不执行
    if (!isMounted) return;
    
    // 创建新的 AbortController
    abortController = new AbortController();
    
    try {
      // ... fetch logic with abortController.signal ...
    } catch (err) {
      if (!isMounted) return; // 组件已卸载，忽略错误
      // ... error handling ...
    }
  };
  
  fetchData();
  
  return () => {
    isMounted = false;
    // 取消所有进行中的请求
    if (abortController) {
      abortController.abort();
    }
    // 清理重试定时器
    if (retryTimeoutId) {
      clearTimeout(retryTimeoutId);
    }
  };
}, [initialEvents.length, initialConferences.length]); // 只在初始数据变化时重新运行
```

### 修复3: 添加 Next.js 内存限制

**文件**: `frontend/Dockerfile.prod`

确保 Node.js 内存限制正确设置：
```dockerfile
CMD ["sh", "-c", "echo 'Starting Next.js server...' && NODE_OPTIONS='--max-old-space-size=2048 --enable-source-maps' node server.js"]
```

### 修复4: 改进健康检查

**文件**: `docker-compose.prod.yml`

```yaml
healthcheck:
  test: ["CMD", "sh", "-c", "wget --no-verbose --tries=1 --spider --timeout=5 http://127.0.0.1:3000/api/health 2>&1 | grep -qE '200|saved|HTTP' && wget --no-verbose --tries=1 --spider --timeout=10 http://127.0.0.1:3000/ 2>&1 | grep -qE '200|saved|HTTP' || exit 1"]
  interval: 30s
  timeout: 15s
  retries: 3
  start_period: 300s
```

这会同时检查健康端点和首页，确保首页也能响应。

### 修复5: 添加进程监控和自动重启

如果进程崩溃但容器未重启，需要更严格的监控。

## 诊断步骤

1. **检查内存使用趋势**
```bash
docker stats conference-frontend-prod
```

2. **检查未清理的定时器**
```bash
# 在容器内检查
docker exec conference-frontend-prod ps aux | grep node
```

3. **检查SSR日志**
```bash
docker logs --tail 200 conference-frontend-prod | grep -E "SSR|timeout|Error|Memory"
```

4. **检查进程崩溃**
```bash
docker inspect conference-frontend-prod | grep -E "RestartCount|State"
```

5. **检查连接数**
```bash
docker exec conference-frontend-prod netstat -an | grep :3000 | wc -l
```

