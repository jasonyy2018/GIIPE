# 优化SSR渲染速度 - 解决端口3000响应慢问题

## 问题分析

从诊断结果看：
- ✅ 服务器正常运行
- ✅ 端口映射正常
- ✅ 从容器IP访问成功（返回123KB HTML，304 Not Modified）
- ⚠️ 但容器内访问根路径时wget没有输出（可能超时）

**关键发现**：服务器实际上在工作，但SSR渲染可能太慢，导致：
1. 浏览器超时
2. wget等待时间过长
3. 用户看到空白页面

## 已完成的优化

### 1. 减少SSR超时时间
- 从15秒减少到8秒
- 超时后立即渲染页面（使用空数据），客户端会重新获取

### 2. 减少API请求超时
- 从10秒减少到5秒
- 更快失败，更快渲染

### 3. 改进超时处理
- 超时不再抛出错误，而是返回空数据
- 确保页面总是能渲染，即使数据获取失败

## 验证优化

重新构建后测试：

```bash
# 重新构建
docker-compose -f docker-compose.prod.yml build --no-cache frontend
docker-compose -f docker-compose.prod.yml up -d frontend

# 等待启动
sleep 30

# 测试响应时间
time curl -s http://localhost:3000/ > /dev/null
```

## 如果仍然慢

可以考虑：

1. **禁用SSR，使用客户端渲染**
   - 将 `page.tsx` 改为客户端组件
   - 数据在客户端获取

2. **使用静态生成（ISG）**
   - 预渲染页面
   - 定期更新

3. **优化API响应时间**
   - 检查后端API性能
   - 添加缓存

## 当前配置

- SSR超时：8秒
- API超时：5秒
- 超时后：立即渲染页面（空数据），客户端会获取

这样确保页面总是能快速渲染，即使数据获取慢。

