# 首页空白问题诊断和修复

## 诊断步骤

在服务器上运行以下命令进行诊断：

```bash
# 全面诊断
bash 诊断首页空白问题.sh

# 快速诊断
bash 快速诊断首页空白.sh
```

## 可能的原因

### 1. SSR渲染失败
- API请求超时
- 后端连接失败
- 数据获取错误

### 2. 组件渲染错误
- AuthContext错误
- PublicLayout组件错误
- 客户端JavaScript错误

### 3. 网络问题
- Nginx配置问题
- 代理超时
- 静态资源加载失败

### 4. 浏览器端问题
- JavaScript被禁用
- 浏览器扩展干扰
- 缓存问题

## 已完成的修复

### 1. 改进PublicLayout错误处理
- 添加了AuthContext的错误处理
- 确保即使AuthContext有问题也能渲染

### 2. 创建诊断脚本
- `诊断首页空白问题.sh` - 全面诊断
- `快速诊断首页空白.sh` - 快速检查

### 3. 创建测试页面
- `/test-simple` - 简单的测试页面，用于验证服务器是否正常

## 检查清单

### 在服务器上检查：

1. **检查容器状态**
   ```bash
   docker ps | grep frontend
   ```

2. **检查前端日志**
   ```bash
   docker logs --tail 200 conference-frontend-prod
   ```

3. **测试直接访问前端**
   ```bash
   docker exec conference-frontend-prod wget -q -O- http://127.0.0.1:3000/ | head -50
   ```

4. **测试健康检查端点**
   ```bash
   docker exec conference-frontend-prod wget -q -O- http://127.0.0.1:3000/api/health
   ```

5. **检查后端连接**
   ```bash
   docker exec conference-frontend-prod wget -q -O- http://backend:3001/health
   ```

### 在浏览器中检查：

1. **打开开发者工具（F12）**
   - Console标签：查看JavaScript错误
   - Network标签：查看资源加载情况
   - 检查是否有404或500错误

2. **检查页面源码**
   - 右键 -> 查看页面源代码
   - 检查HTML是否完整
   - 检查是否有内容

3. **清除缓存**
   - Ctrl+Shift+R (硬刷新)
   - 或清除浏览器缓存

## 常见问题和解决方案

### 问题1: HTML为空或只有基本结构
**原因**: SSR渲染失败
**解决**: 
- 检查后端API是否可访问
- 检查SSR日志中的错误
- 增加API超时时间

### 问题2: 页面加载但显示空白
**原因**: 客户端JavaScript错误
**解决**:
- 查看浏览器控制台错误
- 检查是否有组件渲染错误
- 检查AuthContext是否正常

### 问题3: 部分内容显示，部分空白
**原因**: 某些组件渲染失败
**解决**:
- 检查具体是哪个组件
- 查看组件日志
- 检查数据格式是否正确

## 下一步

1. 运行诊断脚本获取详细信息
2. 查看浏览器控制台错误
3. 检查前端日志
4. 根据错误信息进行针对性修复

